document.addEventListener(function(){
    const tikcetPrice = 550;
    const maxTikcets = 4;
    let selectedTickets = 0;

    const ticketButtons = document.querySelectorAll(".price");
    const totalPriceElement = document.querySelectorAll(".grid-cols-3:last-child")
})